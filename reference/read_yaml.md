# Alias for yaml::read_yaml with a custom handler

Alias for yaml::read_yaml with a custom handler

## Usage

``` r
read_yaml(path)
```

## Arguments

- path:

  Path to yaml file

## Value

A list

## Examples

``` r
if (FALSE) { # \dontrun{
read_yaml(path)
} # }
```
